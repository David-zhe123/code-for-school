<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.UserMapper">

    <resultMap id="UserMap" type="com.travel.pojo.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="UserMap">
        select * from t_user where id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserMap">
        select * from t_user where username = #{username}
    </select>

    <select id="findAll" resultMap="UserMap">
        select * from t_user order by id desc
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into t_user (username, password_hash, status, created_at, updated_at)
        values (#{username}, #{passwordHash}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateStatus">
        update t_user
        set status = #{status}, updated_at = now()
        where id = #{id}
    </update>

</mapper>

